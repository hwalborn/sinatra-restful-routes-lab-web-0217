<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>All Recipes</title>
  </head>
  <body>
    <% @recipes.each do |recipe| %>
      <h2><%= recipe.name %></h2>
      <p>/recipes/<%= recipe.id %></p>
    <% end %>
  </body>
</html>
